"""Optional extensions for MiniAgent.

These modules are not imported by default — import them explicitly:

    from miniagent.extensions.mcp_client import MCPClient, load_mcp_tools
    from miniagent.extensions.orchestrator import Orchestrator
"""
